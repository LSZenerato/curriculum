import React from 'react';
import { SecondSection } from '../../organisms';

function Tecnicas({ SecondSectionProps }) {

  return <SecondSection loading="lazy" props={SecondSectionProps} />
}

export default Tecnicas;